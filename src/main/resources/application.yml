# File upload directory
file:
  upload-dir: uploads
server:
  port: 8080

spring:
  application:
    name: finance-tracker-backend

  data:
    mongodb:
      uri: mongodb+srv://Moody:eZ]EGTV7C3~2QY-@cluster0.dz40wrp.mongodb.net/finance_tracker?retryWrites=true&w=majority
      database: finance_tracker

  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:mySecretKeyForJwtTokenGenerationWhichShouldBeVeryLongAndSecure2024}
  expiration: 86400000  # 24 hours in milliseconds

# Logging
logging:
  level:
    root: INFO
    com.financetracker: DEBUG
    org.springframework.security: INFO
    org.springframework.data.mongodb: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Email Configuration
spring:
  mail:
    host: smtp.example.com
    port: 587
    username: tharkimon23@gmail.com
    password: Th@050106
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
